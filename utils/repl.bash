#!/usr/bin/env bash

# ---------------------------------------------------
synopsis="repl FLAKE"
help="Enter a repl with the flake's outputs"
description="""
"""
# ---------------------------------------------------

cmd () {
  repl ${@}
}
